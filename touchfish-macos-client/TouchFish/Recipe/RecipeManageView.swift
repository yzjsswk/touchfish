import SwiftUI

struct RecipeManageView: View {
    
    var body: some View {
        VStack {
            Spacer()
            Text("Not   Avaliable")
                .foregroundStyle(.gray)
                .font(.largeTitle)
                .offset(x:0, y:-60)
            Spacer()
        }
    }
    
}
